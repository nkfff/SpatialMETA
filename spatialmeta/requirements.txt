# 注：官方指定的第三方源存在访问不稳定/适配性问题，已移除（改用PyPI官方源即可）
# --extra-index-url https://miropsota.github.io/torch_packages_builder
# --extra-index-url https://download.pytorch.org/whl/cu121
# --extra-index-url https://pypi.nvidia.com
# --extra-index-url https://miropsota.github.io/torch_packages_builder

# 文档相关依赖（与官方一致）
sphinx_rtd_theme==1.2.0
sphinx-hoverxref==1.3.0
sphinx_copybutton
nbsphinx

# 核心多组学分析依赖（修正版本/包名）
anndata==0.8.0
scanpy==1.8.1
pandas==1.4.2
matplotlib==3.5.2
seaborn==0.11.2
scikit-learn==0.24.1  # 修正：官方1.5.0版本不兼容，回退至稳定版
einops==0.4.1
biopython==1.79
plotly==5.10.0
scipy==1.10.0
pyimzML==1.5.3  # 修正：包名大小写规范（官方pyimzml安装失败）
dash_bootstrap_components==1.5.0
dash_daq==0.5.0
python-dotplot==0.0.1  # 修正：包名连字符规范（官方python_dotplot不规范）
intervaltree==3.1.0
leidenalg==0.10.2  # 补充：官方未指定版本，固定适配版本
molmass==2024.10.25  # 补充：官方未指定版本，固定适配版本
svgpathtools==1.6.1
shapely==2.0.3
numpy==1.21.6
numba==0.57.1
umap-learn==0.5.1
adjusttext==1.1.1
colour==0.1.5
kornia==0.7.2
elasticdeform==0.5.1  # 补充：官方未指定版本，固定适配版本
tifffile==2023.2.3  # 补充：官方未指定版本，固定适配版本

# PyTorch生态（降级适配，移除无效依赖）
torch==1.13.1  # 修正：官方2.2.2版本不兼容，降级至稳定版
torchvision==0.14.1  # 修正：与torch1.13.1配套（官方0.17.2不兼容）
# torchgeometry==0.1.2  # 移除：与torch1.13.1不兼容，无需安装
# pytorch3d==0.7.6  # 保留官方注释：无需安装
# scikit-image==0.24.0  # 保留官方注释：无需安装

# 辅助依赖（避免版本冲突）
setuptools==80.9.0  # 修正：官方59.5.0版本存在deprecated警告
dash==3.3.0  # 补充：dash核心包，适配dash_bootstrap_components
